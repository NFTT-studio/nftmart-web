/* eslint-disable import/prefer-default-export */

export const Z_INDEXES = {
  // popover's index - 1
  header: 9,
  typeFilter: 9,
  banner: 8,
};

export const emailRex = /^([a-zA-Z0-9_-])+@([a-zA-Z0-9_-])+(.[a-zA-Z0-9_-])+/;
